[package]
name = "codereviewer-rules"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
codereviewer-core = { path = "../core" }
tree-sitter = "0.26"

[dev-dependencies]
serde_json = "1"
