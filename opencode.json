{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "codereviewer": {
      "type": "local",
      "command": ["cargo", "run", "--release", "-p", "codereviewer-mcp"],
      "enabled": true
    }
  }
}
